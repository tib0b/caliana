"""[Phase 2] Standalone PyQt application entry point. SPEC.md §1 (phasing).

The app wraps the same widgets used from notebooks (SPEC §2.2) into a single
window with installer-grade packaging and error handling for non-coders.
Deferred to Phase 2; this is a placeholder so the entry point exists.
"""
from __future__ import annotations


def main() -> None:
    raise NotImplementedError("The standalone app is Phase 2 (SPEC.md §1).")


if __name__ == "__main__":
    main()
